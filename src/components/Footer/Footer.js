import './Footer.css'

export default function Footer() {
    return (
        <footer class="footer">
            <span style={{paddingLeft: '3%'}}>dinnerdash | todos os direitos reservados</span>
            <span style={{paddingLeft: '50%'}}>desenvolvido por um trainee legal</span>
        </footer>
    );
}